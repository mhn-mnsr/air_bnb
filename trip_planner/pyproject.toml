[project]
name = "trip_planner"
version = "0.1.0"
description = "CrewAI workflow-based trip planner"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "crewai>=0.193.0",
  "python-dotenv>=1.0.1",
  "openai>=1.0.0"
]

[project.scripts]
trip-planner = "trip_planner.main:run"

[tool.uv]
dev-dependencies = []
